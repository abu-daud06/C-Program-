#include<stdio.h>
#include<conio.h>
 int main()
 {
   int h,w;
   float area;
   printf("Enter the value of H & W:");
   scanf("%d%d",&h,&w);
   area=.5*h*w;
   printf("Area is:%f",area);
   getch();
   return 0;
 
 }
 
